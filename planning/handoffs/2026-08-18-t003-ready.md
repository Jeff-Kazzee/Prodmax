# Prodmax pickup — 2026-08-18 (T-003 ready)

**For:** the next coding agent or human with a terminal.
**Repo:** `C:\Users\jeffk\big-projects\Prodmax`
**Remote:** `https://github.com/Jeff-Kazzee/Prodmax` (public)
**Checkout:** `dev` (GitHub default). Tree clean. No in-flight ticket. No
dev server assumed running.

This file is the pickup brief. Binding protocol lives in `AGENTS.md` and
`planning/tickets/README.md` — do not re-derive it here.

## Start here

1. Checkout `dev`, `git pull origin dev`. Done when `git status -sb` is
   `## dev...origin/dev` with a clean tree.
2. Read `AGENTS.md`, then `planning/tickets/README.md` (branch + claim
   rules). Done when you can name the next ticket and the PR target.
3. Claim **T-003** (`planning/tickets/T-003-issue-views-ui.md`): set
   `status: claimed`, add `assignee:`. Done when the ticket header shows
   claimed and the index row in `planning/tickets/README.md` matches.
4. Cut `feat/t-003-issue-views` from `dev`. Done when that branch is
   current and tracks no `prod` / `main` work.

## Git (binding)

| Branch | Role |
|---|---|
| `dev` | Integration. GitHub default. **Only merge target for feature PRs.** |
| `prod` | Finished product. Promote `dev` → `prod` **only when Jeff asks to release.** |
| `feat/*` `fix/*` `docs/*` | Work. Cut from `dev`. |
| `main` | Historical. Do not land work here. |

`origin/dev` = `549c290` (PR #4). `origin/prod` = `5abb218` (one docs
commit behind `dev`; leave it). Conventional atomic commits on the feature
branch; PR **into `dev` only**.

Leave [PR #2](https://github.com/Jeff-Kazzee/Prodmax/pull/2) (`ecc-tools`
bundle) alone unless Jeff says otherwise.

## What is already done

- **T-001** docs: AI dock + local CLI providers (`claude-code` / `codex`).
- **T-002** issues/comments/views/undo API on `dev` (`effe2b7` + follow-ups).
- **D-001** closed: `undo_tokens` is migration `0001_undo_tokens`; bulk+undo
  are one transaction; `move_team` undo drops the redirect.
- M0 scaffold, M1 schema/auth/workspaces/teams/states/labels, M2 shell
  (routes still show `ScreenPending` for unbuilt screens).

Last verified gates on the T-002 follow-up: `npm run check` 0 errors ·
`npm test` 96/96 · `npm run build` clean · `npm run e2e` 6/6.

Demo: `demo@prodmax.dev` / `prodmax-demo`. After login, wait for
`nav[aria-label="Workspace sections"]` before keys. ≤6 e2e logins.

## Next ticket (T-003)

Issue views UI. `depends-on: T-002` (done). Owns
`src/island/features/issues/**`, `src/components/issues/**`, route swaps in
`src/island/app/routes.ts` + `src/island/app.tsx`, and
`tests/island/features/issues*`. Team sidebar: edit **link targets only**
in `src/island/components/shell/teams-section.tsx`.

Consume the T-002 API (`?wsId=`, `filters` JSON, views CRUD, bulk +
`POST /api/undo/:token`). Rows open `?issue=` only — the panel is **T-004**.
Optimistic UI per ux-spec §3.0. Ticket is large; land coherent slices
(filter+list, then board, then table) as atomic commits on the same
feature branch if needed.

## Gaps that are allowed to wait

- Event bus is a no-op choke-point (`recordIssueMutation`) until **T-016**.
- Create-issue `suggestions: []` until **T-011**.
- Extra teams from `POST /api/teams` do not provision states; T-003 tests
  that need a second team must seed states (see `tests/api/issues-more.test.ts`).

## Environment

Path is apostrophe-free. `scripts/with-subst.mjs` and
`scripts/patch-astro.mjs` are dormant no-ops; keep them. Kill servers by
port PID (`taskkill //PID <pid> //F` in Git Bash), never a broad node kill.
Preserve Microsoft Defender.

## Exit of this pickup

The next session has claimed T-003 on a feature branch from `dev` and is
writing against the ticket's acceptance list. This file is then stale —
write a new dated handoff when you stop again.
