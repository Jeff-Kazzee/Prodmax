# Prodmax pickup — 2026-08-18 (T-005 ready)

**For:** the next coding agent or human with a terminal.
**Repo:** `C:\Users\jeffk\big-projects\Prodmax`
**Remote:** `https://github.com/Jeff-Kazzee/Prodmax` (public)
**Checkout:** `dev` after T-004's PR is merged (it is). Do not land on
`prod` or `main`. Leave [PR #2](https://github.com/Jeff-Kazzee/Prodmax/pull/2)
alone.

This file is the pickup brief. Binding protocol lives in `AGENTS.md` and
`planning/tickets/README.md`.

## Start here

1. Checkout `dev`, `git pull origin dev`. Done when `git status -sb` is
   `## dev...origin/dev` with a clean tree.
2. Read `AGENTS.md`, then `planning/tickets/README.md`. Done when you can
   name the next ticket and the PR target (`dev` only).
3. Claim **T-005** (`planning/tickets/T-005-projects-api.md`): set
   `status: claimed`, add `assignee:`. Cut `feat/t-005-projects-api` from
   `dev`. Done when that branch is current.

## Git (binding)

| Branch | Role |
|---|---|
| `dev` | Integration. GitHub default. **Only merge target for feature PRs.** |
| `prod` | Finished product. Promote `dev` → `prod` **only when Jeff asks to release.** |
| `feat/*` `fix/*` `docs/*` | Work. Cut from `dev`. |
| `main` | Historical. Do not land work here. |

`origin/dev` = `bb9afe4` (PR #7, T-004). `origin/prod` = `5abb218` (leave it).
Conventional atomic commits on the feature branch. PR **into `dev` only**.
Do not merge without Jeff's approval (SOP 04).

## What is already done

- **T-001…T-004** on `dev`: spec amendments, issues API, issue views, issue
  panel + new-issue modal + triage.
- Panel portals into `data-panel-slot`. Create is `C`/`V` via ShellState.
- CSRF Origin matching treats localhost and 127.0.0.1 as the same host
  (needed for cookie POSTs against `npm run preview`).
- Issue writes already go through `recordIssueMutation` /
  `onIssueMutation` in `src/lib/services/issues-events.ts`. That is the
  registration hook T-005 should use for `progress_cache` so you stay
  inside the owns list.

Last gates on T-004: `npm run check` 0 errors · `npm test` 121/121 ·
`npm run build` clean · `npm run e2e` 8/8.

Demo: `demo@prodmax.dev` / `prodmax-demo`. After login, wait for
`nav[aria-label="Workspace sections"]` before keys. ≤6 e2e logins.

## Next ticket (T-005)

Projects/cycles **API**. `depends-on: T-002` (done). Owns
`src/pages/api/{projects,project-updates,milestones,cycles}/**`,
`src/lib/services/{projects,project-updates,milestones,cycles}*`,
`src/lib/validation/{projects,cycles}*`, `tests/api/{projects,cycles}*`.
Schema already exists. UI is **T-006**.

Ticket is large. Land coherent slices as atomic commits on the same
branch (projects CRUD + cache hook, then milestones, then cycles
scope/close/rollover).

Before calling the ticket done, run a thermo-nuclear code-quality review
of the branch vs `origin/dev` and fix structural issues it finds. Same
bar as T-004. Not on every tiny commit.

## Gaps that are allowed to wait

- Favorites in the sidebar (SB-03) still needs an M2 constraint amendment.
- Issue panel omits project/cycle/milestone chips until **T-006**.
- Drafts are localStorage. Attachments tab is empty honest copy. Dedup
  404 is silent until **T-012**. Triage snooze is local until **T-016**.
- SSE from `recordIssueMutation` waits **T-016**.
- **T-007** (docs API) is file-disjoint and may run in a second session.
  Serialize DB migrations at the integration checkpoint.

## Environment

Path is apostrophe-free. `scripts/with-subst.mjs` and
`scripts/patch-astro.mjs` are dormant no-ops. Keep them. Kill servers by
port PID (`taskkill //PID <pid> //F`), never a broad node kill. Preserve
Microsoft Defender.

## Exit of this pickup

The next session has claimed T-005 on a feature branch from `dev` and is
writing against the ticket's acceptance list. This file is then stale.
Write a new dated handoff when you stop again.
