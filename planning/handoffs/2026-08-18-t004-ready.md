# Prodmax pickup — 2026-08-18 (T-004 ready)

**For:** the next coding agent or human with a terminal.
**Repo:** `C:\Users\jeffk\big-projects\Prodmax`
**Remote:** `https://github.com/Jeff-Kazzee/Prodmax` (public)
**Checkout:** `dev` after T-003's PR merges. Do not land on `prod` or `main`.
Leave [PR #2](https://github.com/Jeff-Kazzee/Prodmax/pull/2) alone.

This file is the pickup brief. Binding protocol lives in `AGENTS.md` and
`planning/tickets/README.md`.

## Start here

1. Merge / checkout latest `dev` (`git pull origin dev`).
2. Claim **T-004** (issue panel + new-issue modal + triage): `status: claimed`,
   `assignee:`, cut `feat/t-004-issue-panel` from `dev`.
3. Rows already open `?issue=` from T-003 — T-004 owns the panel itself
   (S-12) plus S-13 create modal and S-16 triage.

## What T-003 left on the bench

- Favorites in the sidebar (SB-03) — component/star exist on the view chrome;
  mounting in `sidebar.tsx` needs an M2 constraint amendment.
- View controls live in the issues screen, not the topbar SB-11 cluster.
- Column "+" / `C` create-issue is a toast pointing here.
- AI NL filter chips wait for T-011. SSE row patches wait for T-016.

## Gates on the T-003 branch

`npm run check` 0 errors · `npm test` 114/114 · `npm run build` clean ·
`npm run e2e` 7/7. Demo: `demo@prodmax.dev` / `prodmax-demo`.
